"""
Main file to test the functionalities the coupled simulations.
To be converted in to a real test file, but pytest might lead to problems with the multiprocessing/shared memory etc.
"""

def main ():
    """

    :return:
    """
    # Init all the files and paths

    # generate the ep simulation instance objects

    # init the Manager and the shared memory

    # Run the simulation in parallel with concurrent.futures.ProcessPoolExecutor


if __name__ == "__main__":
    main()